import 'package:flutter/material.dart';

class CustomTheme {
  Color primaryColor;
  String title;
  String fontFamily;
  String appName;
  Color slaveColor;
  Color anchorColor;
  Color appBarIconThemeColor;

  CustomTheme(
      {this.primaryColor,
      this.title,
      this.fontFamily,
      this.appName,
      this.slaveColor,
      this.anchorColor,
      this.appBarIconThemeColor});
}
